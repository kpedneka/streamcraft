Review the current diff (uncommitted changes, or the diff between this branch and main if working tree is clean) against `docs/code-review-guidelines.md`.

For each relevant guideline section, either:
- confirm it's satisfied and briefly say how, or
- flag a specific problem with the file/line and a concrete fix

Do not rewrite the code yourself unless asked. End with:
1. A verdict: ready to merge / needs changes / needs discussion
2. Whether this change needs an ADR entry (see `docs/adr/TEMPLATE.md`)
3. Which backlog item(s) in the StreamCraft Notion project this closes or advances
