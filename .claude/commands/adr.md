Draft a new Architecture Decision Record for: $ARGUMENTS

1. Copy `docs/adr/TEMPLATE.md` to `docs/adr/NNN-short-title.md` (next available number, zero-padded, e.g. `001-ffmpeg-invocation-strategy.md`).
2. Fill in Context, Decision, Alternatives considered, and Consequences based on the conversation so far — ask if anything is unclear rather than guessing.
3. Present the draft for confirmation before treating it as final.
4. Once confirmed, remind the human to mirror the decision into the "Architecture Decision Log" page in the StreamCraft Notion project.
